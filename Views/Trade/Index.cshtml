@model List<RendaVariavelViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2 class="text-center">Compra de Ações</h2>
</br>


<fieldset id="areaFiltro" class="form-group">
    <legend>Filtro</legend>
    <div class="row">
        <div class="col-lg-4">
            Sigla<br />
            <input type="text" id="sigla" class="form-control" />
        </div>
        <div class="col-lg-1">
            <br />
            <input type="button" id="btnFiltro" class="btn btn-success" value="Aplicar"
                   onclick="aplicaFiltroConsultaTrade()" />
        </div>
    </div>
</fieldset>

</br>
</br>

<div class="col-lg-3">
    Quantidade:<br />
    <input type="number" id="qtd" class="form-control" />
</div>

</br>

<div id="resultadoConsulta" class="table-responsive">
    <table class="table mx-auto">
        <thead>
            <tr>
                <th scope="col">Ícone</th>
                <th scope="col">Sigla</th>
                <th scope="col">Preço</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var renda in Model)
            {
                <tr>
                    <td>
                        <img id="imgPreview" src="data:image/jpeg;base64,@renda.ImagemEmBase64" class="img-responsive"
                             width="50">
                    </td>
                    <td>@renda.Sigla</td>
                    <td>@renda.Preco</td>
                    <td>
                        <a onclick="confirmarCompra(@renda.Id)" class="btn btn-success">Comprar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>